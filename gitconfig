[alias]
  co = checkout
  ci = commit -S
  st = status
  br = branch
  tag = tag -s
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  lc = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit -p
  type = cat-file -t
  dump = cat-file -p
  branches = branches
  undo = undo
  sync = sync
  sy = sync
  resync = resync
  rs = resync
  switch = switch
  sw = switch
  publish = publish
  pub = publish
  unpublish = unpublish
  unp = unpublish

[user]
  name = Kura
  email = 
  signingkey = /home/kura/.ssh/id_ed25519

[github]
  user = kura
  token = 

[core]
  excludesfile = /home/kura/.gitignore
  editor = nano

[hub]
  protocol = ssh

[push]
  default = matching

[transfer]
  fsckobjects = true

[fetch]
  fsckobjects = true

[receive]
  fsckObjects = true

[icdiff]
  options = --highlight --line-numbers

[pager]
  diff = delta
  log = delta
  reflog = delta
  show = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  features = side-by-side line-numbers decorations
  whitespace-error-style = 22 reverse

[delta "decorations"]
  commit-decoration-style = bold yellow box ul
  file-style = bold yellow ul
  file-decoration-style = none

[init]
  defaultBranch = main

[gpg]
  format = ssh

[gpg "ssh"]
  allowedSignersFile = /home/kura/.gitallowedsigners
